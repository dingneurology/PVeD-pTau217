# Local data directories

`raw/` and `derived/` are intentionally empty in the public repository and are
ignored by Git. They are for authorised local use only.

Do not override the ignore rules to commit cohort data.
